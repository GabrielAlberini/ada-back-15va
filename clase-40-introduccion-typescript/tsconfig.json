{
  "compilerOptions": {
    "target": "es5", // Especifica la versión de ECMAScript a la que se compilará el código TypeScript
    "module": "commonjs", // Define el sistema de módulos a utilizar (por ejemplo, "commonjs", "amd", "es6", etc.)
    "strict": true, // Habilita todas las opciones de comprobación estricta de TypeScript
    "esModuleInterop": true, // Habilita la interoperabilidad de espacios de nombres de CommonJS y módulos de ECMAScript
    "skipLibCheck": true, // Omite la comprobación de librerías de definiciones de tipos al compilar
    "forceConsistentCasingInFileNames": true, // Garantiza que los nombres de archivo tengan un formato consistente
    "outDir": "./dist" // Directorio de salida para los archivos compilados
  },
  "include": [
    "*.ts" // Patrón que especifica los archivos que se deben incluir en la compilación
  ],
  "exclude": [
    "node_modules" // Carpetas o archivos que se deben excluir de la compilación
  ]
}
